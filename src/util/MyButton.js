import React from 'react';
//import { Icon } from 'react-native-elements'
import { Button } from 'react-native';

export default ({ onClick,tipStyle }) => (
  <Button style={tipStyle} onClick={onClick}>
  </Button>
);
